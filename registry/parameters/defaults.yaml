# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: registry
  rootgroup: root
  config: '/etc/registry'
  service:
    name: registry
  subcomponent:
    config: '/etc/registry-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
